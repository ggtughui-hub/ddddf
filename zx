-- قم بنسخ هذا الكود ولصقه في محقن Delta

local player = game.Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- التأكد من عدم تكرار الواجهة
if playerGui:FindFirstChild("DeltaMenuGui") then
	playerGui.DeltaMenuGui:Destroy()
end

-- إنشاء واجهة المستخدم (GUI)
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "DeltaMenuGui"
screenGui.ResetOnSpawn = false
screenGui.Parent = playerGui

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 200, 0, 130)
frame.Position = UDim2.new(0.5, -100, 0.7, 0)
frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
frame.BorderSizePixel = 0
frame.Active = true
frame.Draggable = true -- سحب القائمة في أي مكان بالشاشة
frame.Parent = screenGui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 10)
corner.Parent = frame

-- عنوان القائمة
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 30)
title.BackgroundTransparency = 1
title.Text = "التحكم الثابت في الارتفاع"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.TextSize = 14
title.Font = Enum.Font.SourceSansBold
title.Parent = frame

-- زر التزويد 5+ (نزول وثبات)
local plusButton = Instance.new("TextButton")
plusButton.Size = UDim2.new(0.85, 0, 0, 35)
plusButton.Position = UDim2.new(0.075, 0, 0, 40)
plusButton.BackgroundColor3 = Color3.fromRGB(0, 162, 255)
plusButton.Text = "تزويد 5+ (نزول)"
plusButton.TextColor3 = Color3.fromRGB(255, 255, 255)
plusButton.TextSize = 14
plusButton.Font = Enum.Font.SourceSansBold
plusButton.Parent = frame

local btnCorner1 = Instance.new("UICorner")
btnCorner1.CornerRadius = UDim.new(0, 8)
btnCorner1.Parent = plusButton

-- زر التنقيص 5- (صعود وثبات)
local minusButton = Instance.new("TextButton")
minusButton.Size = UDim2.new(0.85, 0, 0, 35)
minusButton.Position = UDim2.new(0.075, 0, 0, 82)
minusButton.BackgroundColor3 = Color3.fromRGB(255, 70, 70)
minusButton.Text = "تنقيص 5- (صعود)"
minusButton.TextColor3 = Color3.fromRGB(255, 255, 255)
minusButton.TextSize = 14
minusButton.Font = Enum.Font.SourceSansBold
minusButton.Parent = frame

local btnCorner2 = Instance.new("UICorner")
btnCorner2.CornerRadius = UDim.new(0, 8)
btnCorner2.Parent = minusButton

-- دالة تعديل الارتفاع مع تجميد السرعة لضمان الثبات
local function adjustHeight(amount)
	local character = player.Character
	if not character or not character:FindFirstChild("HumanoidRootPart") or not character:FindFirstChildOfClass("Humanoid") then return end
	
	local humanoidRootPart = character.HumanoidRootPart
	local humanoid = character.Humanoid
	
	local currentPos = humanoidRootPart.Position
	local targetY = currentPos.Y - amount
	
	-- نقل اللاعب للمكان الجديد
	humanoidRootPart.CFrame = CFrame.new(currentPos.X, targetY, currentPos.Z)
	
	-- تثبيت اللاعب ومنع الجاذبية من سحبه عبر إيقاف السرعة المؤقتة
	humanoidRootPart.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
	humanoidRootPart.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
end

-- ربط الأزرار بالوظائف
plusButton.MouseButton1Click:Connect(function()
	adjustHeight(5)
end)

minusButton.MouseButton1Click:Connect(function()
	adjustHeight(-5)
end)
